<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/bootstrap.min.css" type="text/css" />
  <link rel="stylesheet" href="/css/font-awesome.min.css" type="text/css" />
  <link rel="stylesheet" href="/css/app.css" type="text/css" />
  <title><%= appname %></title>
  <style>
    .bg-primary {
      background-color: #3f51b5 !important;
    }
    .btn-primary {
      background-color: #3f51b5 !important;
    }
    .text-primary {
      color: #3f51b5 !important;
    }
  </style>
</head>
<body>
 <nav class="navbar navbar-expand-sm bg-primary text-white py-0 text-center d-flex justify-content-around">
    <a class="navbar-brand text-white text-center" href="#"><i class="fa fa-medkit icon" aria-hidden="true"></i></a>
    <h4 class="font-weight-normal app-title d-inline"><%= appname %>
    </h4>
  </nav>
  <main class="container-fluid" style="background: url(/images/background.jpg) no-repeat; background-size: cover; min-height: 90vh;">
    <div class="d-flex justify-content-between align-items-center">
      <div>
        <span class="mr-1">Welcome, </span>
        <strong class="mr-3"><%= 'Obidi' %></strong>
      </div>
      <a href="/mobile/users/logout" class="text-danger">Logout</a>
    </div>
    <section class="row space">
      <% doctors.forEach(d => { %>
        <div class="bg-white w-100 p-2 mx-2" style="box-shadow: 0px 2px 5px black; border-radius: 15px;">
          <div class="d-flex">
            <div style="width: 120px; height: 120px;border-radius: 25px;">
              <img src='<%= domain + "/images/" + d.photo %>' class="img-thumbnail">
            </div>
            <div class="px-2">
              <h4><%= d.surname + ' ' + d.other_names %></h4>
              <p><%= d.specialization %></p>
              <p><%= d.work_address %></p>
              <p><%= d.hospital %></p>
            </div>
          </div>
          <div class="p-1 d-flex">
            <a href='<%= "tel:" + d.phone %>' class="btn btn-success text-center p-2 text-white mx-2">
              <strong class="mb-0"><%= d.phone %></strong><br>
              <i class="fa fa-phone" aria-hidden="true"></i>
            </a>
            <a href='<%= "mailto:" + d.email %>' class="btn btn-info text-center p-2 bg-info text-white mx-2">
              <strong class="mb-0"><%= d.email %></strong><br>
              <i class="fa fa-envelope-o" aria-hidden="true"></i>
            </a>
          </div>
          </div>
        </div>
      <% }) %>
    </section>
  </main>

  
  <script src="/js/jquery.min.js"></script>
  <script src="/js/bootstrap.bundle.min.js"></script>
</body>
</html>